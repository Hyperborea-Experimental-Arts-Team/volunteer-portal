import React from 'react';
import { concat } from '../util';

import theme from '../theme.css';
import style from './EventSummary.css';

export default ({ eventInto }) => (
  <div className={style.EventSummary}>
    EVENT SUMMARY
  </div>
);